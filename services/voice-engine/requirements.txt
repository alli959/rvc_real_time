# Core dependencies for RVC Real-time Voice Conversion

# Deep Learning
torch>=2.0.0,<2.6.0
torchaudio>=2.0.0,<2.5.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.1
audioread>=3.0.0
pyworld==0.3.2
praat-parselmouth>=0.4.3
pydub>=0.25.1
resampy>=0.4.2

# Fairseq (for HuBERT) - installed from git to avoid dependency issues
# Will be installed via setup script with --no-deps flag
# fairseq @ git+https://github.com/facebookresearch/fairseq.git@v0.12.2

# Retrieval/Search
# Note: faiss-gpu is installed separately in Dockerfile for GPU builds
# faiss-cpu can be installed manually if needed for CPU-only environments

# Numerical computing
numpy>=1.21.0,<2.0.0
scipy>=1.9.0

# API & Networking
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=11.0
python-multipart>=0.0.6

# Audio I/O (for streaming mode)
pyaudio>=0.2.13
sounddevice>=0.4.6
discord.py[voice]>=2.3.0
PyNaCl>=1.5.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0

# Utilities
tqdm>=4.65.0
colorama>=0.4.6
edge-tts>=6.1.9

# Bark TTS - Native emotion and sound effect support
# Requires GPU (12GB VRAM for full, 2GB with small models)
# Comment out if you don't have GPU or want to use Edge TTS only
# Install from git for latest version with best emotion support
# bark @ git+https://github.com/suno-ai/bark.git

# YouTube audio download - use latest for best anti-bot bypass
yt-dlp>=2025.1.0

# Trainer & Phoneme Analysis (requires espeak-ng system package)
phonemizer>=3.2.1
epitran>=1.24
faiss-cpu>=1.7.4

# TensorBoard for training logging/visualization
tensorboard>=2.14.0

# Matplotlib for spectrogram visualization during training
matplotlib>=3.7.0

# Whisper for speech recognition & phoneme detection
openai-whisper>=20231117
